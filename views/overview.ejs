<%- include("partials/header", { title, active, mode, lastFetched }) %>

<main class="container mx-auto px-6 py-8 max-w-7xl">
  <div class="flex items-baseline justify-between flex-wrap gap-2 mb-8">
    <div>
      <h1 class="page-title">Sledování konkurence</h1>
      <p class="page-sub"><%= today %> · 4 české e-shopy s matracemi sledované denně</p>
    </div>
  </div>

  <!-- KPI cards -->
  <section class="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-10">
    <div class="card kpi-accent card-pad">
      <div class="kpi-label">Produkty Pro Spánek</div>
      <div class="kpi-value"><%= kpi.ourProducts %></div>
      <div class="kpi-foot">v poslední aktualizaci</div>
    </div>
    <div class="card card-pad">
      <div class="kpi-label">Průměr konkurence</div>
      <div class="kpi-value"><%= kpi.avgCompetitor %></div>
      <div class="kpi-foot">produktů na konkurenta</div>
    </div>
    <div class="card card-pad">
      <div class="kpi-label">Aktivní akce</div>
      <div class="kpi-value"><%= kpi.activePromos %></div>
      <div class="kpi-foot">napříč všemi konkurenty</div>
    </div>
    <div class="card card-pad">
      <div class="kpi-label">Služby, kde vedeme</div>
      <div class="kpi-value"><%= kpi.servicesLed %></div>
      <div class="kpi-foot">služby potvrzené pouze Pro Spánkem</div>
    </div>
  </section>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
    <!-- Heatmap -->
    <section class="card lg:col-span-2">
      <div class="card-pad border-b border-line flex items-center justify-between">
        <div>
          <h3>Produkty podle cenového segmentu</h3>
          <p class="text-xs text-ink-mute mt-1">Poslední aktualizace · všechny rozměry</p>
        </div>
        <a href="/portfolio" class="text-sm font-medium text-brand hover:text-brand-dark">Otevřít sortiment →</a>
      </div>
      <div class="card-pad overflow-x-auto">
        <table class="data-table">
          <thead>
            <tr>
              <th>Segment</th>
              <% for (const c of competitors) { %>
                <th class="num <%= c === 'prospanek' ? 'us-row' : '' %>"><%= competitorLabels[c] || c %></th>
              <% } %>
            </tr>
          </thead>
          <tbody>
            <% for (const seg of segments) { %>
              <tr>
                <td class="competitor-cell"><%= seg.label %></td>
                <% for (const c of competitors) {
                     const v = heatmap.matrix[seg.key][c];
                     const ratio = heatmap.max ? v / heatmap.max : 0;
                     let lvl = 0;
                     if (ratio > 0.8) lvl = 5;
                     else if (ratio > 0.6) lvl = 4;
                     else if (ratio > 0.4) lvl = 3;
                     else if (ratio > 0.2) lvl = 2;
                     else if (v > 0) lvl = 1;
                     const cls = c === "prospanek" ? `heat heat-us-${lvl}` : `heat heat-${lvl}`;
                %>
                  <td class="<%= cls %>"><%= v %></td>
                <% } %>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </section>

    <!-- Insights -->
    <section class="card">
      <div class="card-pad border-b border-line">
        <h3>Pozoruhodné dnes</h3>
        <p class="text-xs text-ink-mute mt-1">Odvozené signály z aktuálních dat</p>
      </div>
      <div class="card-pad">
        <% if (!insights.length) { %>
          <p class="text-sm text-ink-mute">Žádné pozoruhodné signály — data se možná ještě načítají.</p>
        <% } else { %>
          <ul class="insight-list">
            <% for (const item of insights) { %>
              <li><span class="insight-dot"></span><span><%= item %></span></li>
            <% } %>
          </ul>
        <% } %>
      </div>
    </section>
  </div>
</main>

<%- include("partials/footer") %>
